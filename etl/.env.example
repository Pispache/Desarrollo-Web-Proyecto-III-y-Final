# Configuración del ETL - Marcador de Baloncesto
#
# Copia este archivo como .env y cambia los valores con tus datos reales
# ⚠️ IMPORTANTE: Nunca subas el archivo .env a Git (tiene contraseñas)

# ============================================
# SQL SERVER (donde están los datos originales)
# ============================================
MSSQL_HOST=sqlserver
MSSQL_PORT=1433
MSSQL_DB=MarcadorDB
MSSQL_USER=etl_user
MSSQL_PASSWORD=TU_PASSWORD_SEGURO_AQUI

# Cadena de conexión completa (no la toques a menos que sepas qué haces)
MSSQL_CS=Driver={ODBC Driver 18 for SQL Server};Server=${MSSQL_HOST};Database=${MSSQL_DB};UID=${MSSQL_USER};PWD=${MSSQL_PASSWORD};Encrypt=yes;TrustServerCertificate=yes

# ============================================
# POSTGRESQL (donde se copian los datos para reportes)
# ============================================
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=reportsdb
POSTGRES_USER=etl_writer
POSTGRES_PASSWORD=TU_PASSWORD_SEGURO_AQUI

# Cadena de conexión completa
POSTGRES_CS=host=${POSTGRES_HOST} port=${POSTGRES_PORT} dbname=${POSTGRES_DB} user=${POSTGRES_USER} password=${POSTGRES_PASSWORD} sslmode=prefer

# ============================================
# CONFIGURACIÓN DEL ETL
# ============================================

# Cada cuánto se sincroniza (en segundos)
# Recomendaciones:
#   - Para desarrollo/pruebas: 60-120 (cada 1-2 minutos)
#   - Para producción: 300-600 (cada 5-10 minutos)
ETL_INTERVAL_SECONDS=300

# Cuántas veces reintentar si falla la conexión
ETL_MAX_RETRIES=3

# Cuánto esperar entre cada reintento (en segundos)
ETL_RETRY_DELAY_SECONDS=5

# ============================================
# CONFIGURACIÓN ADICIONAL (opcional)
# ============================================

# Nivel de detalle de los logs (DEBUG muestra más info, ERROR menos)
LOG_LEVEL=INFO

# Tu zona horaria
TZ=America/Guatemala

# ============================================
# CONSEJOS DE SEGURIDAD
# ============================================
# 1. Usa contraseñas largas y complicadas (mínimo 16 caracteres)
# 2. Cambia las contraseñas cada mes o dos
# 3. Nunca compartas este archivo con nadie
# 4. En producción, usa Docker Secrets en lugar de este archivo
# 5. Activa SSL/TLS en las conexiones de producción
